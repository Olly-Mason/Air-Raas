export async function notifyNewJobPosting(jobId: string): Promise<void> {
  // This is a placeholder function for future email notification implementation
  console.log(`New job posted with ID: ${jobId}. Notification should be sent to account managers.`);
  // In the future, implement actual email sending logic here
}